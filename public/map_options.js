(function(window, google, mapster){
  Mapster.MAP_OPTIONS = {
    center: {
      lat: 37.79,
      lng: -122.43
    },
    zoom: 10,
    mapTypeId: "hybrid",
    disableDefaultUI: true,
    scrollwheel: false
  };
}(window, window.Mapster || (window.Mapster = {})))
